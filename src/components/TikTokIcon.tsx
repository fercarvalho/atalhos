const TikTokIcon = (props) => (
  <svg
    {...props}
    viewBox="0 0 24 24"
    fill="none"
    stroke="currentColor"
    strokeWidth={2}
    strokeLinecap="round"
    strokeLinejoin="round"
  >
    {/* Cole o conteúdo SVG do ícone TikTok aqui */}
  </svg>
);

export default TikTokIcon;
